# 个人日程管理系统 · PRD(第一版)

版本:v1.0 日期:2026-09-23 状态:已确认,作为后续开发的唯一依据

---

## 1. 产品目标和使用场景

### 1.1 产品目标
为单个大学生提供一套**本地、离线、开箱即用**的日程管理工具,把"这学期我有什么课""今天该做什么""接下来还有什么安排"这三件事在一个页面里回答清楚。

优先级排序:**数据不丢 > 信息看得快 > 录入方便 > 视觉美观**。

### 1.2 使用场景
- 早上出门前扫一眼首页:今天几点有课、在哪上、有没有要交的东西
- 临时被约了事情:点课表上空着的格子,30 秒内建好一条日程
- 周中规划本周:在日程页按"本周"筛选,逐条勾选推进
- 换电脑 / 清缓存前:导出 JSON 备份

### 1.3 使用者与设备
- 单人使用,无账号体系,无并发,无权限概念
- 手机(Chrome / Safari,iOS 与 Android)+ 桌面浏览器(Chrome / Edge)双端使用
- 无网络也能完整使用(纯本地文件)

### 1.4 技术形态约束
- **单个 `index.html` 文件**,内含全部 HTML / CSS / JS 与预置课表数据,双击即可在浏览器打开
- 零构建、零外部依赖、不引用 CDN 资源(保证离线可用)
- 持久化只用 `localStorage`,不使用数据库、不使用后端接口

---

## 2. 整体页面框架和导航结构

### 2.1 框架
单页应用(SPA)。三个视图通过顶部/底部导航切换,**不刷新页面、不跳转 URL**。视图状态(课表停在第几周、日程选了哪个筛选)在切换后保留,回切不重置。

### 2.2 导航结构

```
桌面 (>=720px)                          移动 (<720px)
+----------------------------------+   +--------------------+
| 我的日程  [首页][课表][日程] [备份]|   | 我的日程     [备份] |
+----------------------------------+   +--------------------+
|                                  |   |                    |
|           内容区                 |   |      内容区        |
|                                  |   |                    |
+----------------------------------+   +--------------------+
                                       |[首页]|[课表]|[日程]|
                                       +--------------------+
```

- 桌面:顶部栏含品牌名、三个 Tab、周次徽章、备份入口。当前 Tab 有下划线 + 加深底色
- 移动:导航下沉为**底部固定 Tab 栏**,加 `env(safe-area-inset-bottom)` 避开手势条,内容区补等高 `padding-bottom`;顶栏只留品牌名与备份入口,周次徽章移到页面标题行
- 主内容区最大宽度 1120px 居中
- 断点:≥1024 桌面 / 720–1023 平板 / <720 移动。平板与桌面共用周网格布局

### 2.3 视图流转关系

```
首页 ──点"今日课程"行────> 课表(定位当前周)
首页 ──点"+ 新日程"──────> [日程编辑弹层·新建]
首页 ──点待办行──────────> [日程编辑弹层·编辑]
首页 ──点待办复选框──────> 原地切换完成态, 不弹层
课表 ──点空白格/空闲行───> [日程编辑弹层·新建, 预填日期与节次时间]
课表 ──点课程色块────────> [课程详情弹层]
日程 ──点列表项主体──────> [日程编辑弹层·编辑]
日程 ──点列表项复选框────> 原地切换完成态
日程 ──弹层内点删除──────> [二次确认弹层]
任意 ──点顶栏"备份"──────> [数据备份弹层]
```

### 2.4 弹层通用规则
三个弹层(日程编辑、课程详情、数据备份)+ 一个二次确认弹层共用一套遮罩机制:
- 半透明黑底,点遮罩空白处关闭,点卡片内不关闭
- Esc 关闭(桌面)
- 打开时锁定背景滚动
- 桌面:居中模态卡片,宽 `min(420px, 92vw)`
- 移动:底部抽屉,宽 100%,顶部圆角 + 拖拽条,`max-height: 85vh` 内部滚动,操作按钮吸底
- 二次确认弹层两端都是居中卡片

---

## 3. 首页的内容结构

自上而下四个区块。桌面两栏(左 2 / 右 1),移动单列堆叠且顺序为:学期横幅 → 下一个安排 → 今日课程 → 今日待办。

```
+----------------------------------+---------------------------+
| A 学期横幅                        |                           |
| 2026-09-23 星期三  第3周/共19周    |                           |
| ████████░░░░░░░░░░ 进度 15%       |                           |
|                                  [+ 新日程]                  |
+----------------------------------+---------------------------+
| B 今日课程 (2)                   | C 下一个安排               |
| 08:00-09:35  ● 进行中            | 科学研究与学术写作         |
| 可靠性中的机器学习(博)            | 09-25 周五 08:50           |
| 刘杰 · B118 · 学院路              | 计算机房 R1-4093           |
|                                  | 还有 2 天 04:12  [去课表]> |
| 15:50-18:15  ○ 未开始            +---------------------------+
| 物流运输模型与算法                | D 今日待办 (已完成 1/3)    |
|                                  | [ ] 交作业 D2L  14:00[作业]|
+----------------------------------+ | [x] 回复导师邮件 (删除线) |
                                   | [+ 添加待办]             |
                                   +---------------------------+
```

| 区块 | 内容 | 规则 |
|---|---|---|
| A 学期横幅 | 今日公历日期、星期、当前教学周、学期进度条、"+ 新日程"按钮 | 非教学周显示"假期 / 非教学周",进度条置满或置空;移动端按钮改为右下角 FAB |
| B 今日课程 | 当天有课的全部时段,按开始节次升序 | 每行:时间区间、课程名、教师、地点、校区。已结束 → 整行降透明度、时间文字变灰;进行中 → 左侧色条 + "进行中"标记;未开始 → "未开始"。空状态文案:"今天没有课,好好安排一下" |
| C 下一个安排 | 未来最近的"课程时段"与"未完成日程"中取最早者 | 显示标题、日期星期、时间、地点、倒计时。倒计时每 60 秒刷新。课程进行中时该卡片左侧描边高亮。无未来项时显示"暂无安排" |
| D 今日待办 | 日期为今天的日程,未完成在上、已完成在下 | 每行:复选框、标题、时间、分类标签。已完成加删除线 + 灰色。顶部显示"已完成 x / y"。行内"+ 添加待办"打开预填今日日期的新建弹层。空状态:"今天没有待办" |

---

## 4. 每个模块的用途

| 模块 | 用途 | 解决什么问题 |
|---|---|---|
| 首页(总览) | 只读的信息聚合页,不承载编辑能力(仅"新建日程"和"勾选完成"两个动作) | 回答"现在是什么时候、今天有什么、下一步是什么" |
| 课表 | 按教学周查看每周课程安排;从空格子直接建日程 | 回答"这周/第 N 周几点在哪个教室",并处理"某课只在部分周次上"的现实 |
| 日程活动 | 待办事项的完整增删改查与完成度跟踪 | 回答"我要做的事都在哪、做完了几件" |
| 数据备份 | 导出 / 导入 JSON、恢复预置课表、清空数据 | 对冲 localStorage 易丢失的风险,支持换设备 |

三个视图之间的数据关系见第 6 章。

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 数据模型

#### 课程 Course
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | 自动 | 唯一标识 |
| `name` | string | 是 | 课程名称 |
| `teacher` | string | 否 | 任课教师,多人用顿号分隔 |
| `campus` | string | 否 | 校区 / 开课单位 |
| `mode` | string | 否 | 上课方式(面授讲课 / 全线上教学) |
| `color` | string | 否 | 色块颜色,缺省按取色顺序分配 |
| `slots` | Slot[] | 是 | 上课时段数组,可为空数组(无固定时间课程) |

#### 上课时段 Slot
| 字段 | 类型 | 说明 |
|---|---|---|
| `weekStart` / `weekEnd` | number | 起止教学周,闭区间,1–19 |
| `weekday` | number | 1=周一 … 7=周日 |
| `periodStart` / `periodEnd` | number | 起止节次,闭区间,1–14 |

> 一门课含多个 Slot 是必需的:「物流运输模型与算法」有 3 个周次范围不同的时段。Slot 内不含地点字段时继承课程级 `location`;本版按"每个 Slot 各自带 `location`"实现,因为同一门课不同时段地点可能不同。

#### 日程 Event
| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | 自动 | 唯一标识 |
| `title` | string | 是 | 标题 |
| `date` | string | 是 | `YYYY-MM-DD` |
| `startTime` | string | 否 | `HH:MM`,留空表示"全天" |
| `endTime` | string | 否 | `HH:MM`,须晚于 `startTime` |
| `location` | string | 否 | 地点 |
| `category` | enum | 是 | `course` 课程 / `homework` 作业 / `meeting` 会议 / `personal` 个人,默认 `personal`,各自固定配色 |
| `note` | string | 否 | 备注,多行 |
| `done` | boolean | 自动 | 完成状态,默认 false |
| `createdAt` | number | 自动 | 时间戳 |

#### 设置 Settings
| 字段 | 说明 |
|---|---|
| `semesterStart` | 学期第 1 周周一日期,常量 `2026-09-07` |
| `totalWeeks` | 总教学周数,常量 `19` |
| `schemaVersion` | 数据结构版本,用于导入兼容判断 |

### 5.2 存储
- `localStorage` 单键:`schedule-app-v1`,值为 `{ courses, events, settings }` 的 JSON
- 首次打开检测到空值 → 写入附录 A 的 4 门预置课程 + 空日程数组
- 任何增删改后立即序列化写回,并只重绘受影响视图
- 日程排序规则:日期升序 → 有时间者优先于全天 → `startTime` 升序

### 5.3 各模块最必要的操作

| 模块 | 第一版必须支持 |
|---|---|
| 课程 | 预置录入;**编辑**课程基础信息与时段列表(增 / 改 / 删单个时段);删除课程(二次确认);查看课程详情 |
| 日程 | 新建、编辑、删除(二次确认)、勾选完成 / 取消完成、按今天 / 本周 / 全部 / 已完成筛选 |
| 课表 | 上一周 / 下一周 / 回到本周切换(限 1–19 周);按当前周过滤课程;点空白格新建日程并预填;查看课程详情 |
| 首页 | 只读聚合 + 新建日程 + 勾选完成 |
| 备份 | 导出 JSON 文件、导入 JSON 文件(先校验结构再覆盖)、恢复预置课表、清空全部数据 |

### 5.4 表单校验规则
- 标题为空 / 日期为空 → 阻止保存,字段下方红字提示,失焦即校验
- `endTime` ≤ `startTime` → 提示"结束时间需晚于开始时间"并阻止保存
- 课程名必填;时段至少保留 1 条,或明确允许为空(空 = 无固定时间课程)
- 导入 JSON 解析失败 → 红色 toast「文件格式不正确」,现有数据不做任何改动

---

## 6. 首页、今日计划和各模块之间的关系

### 6.1 单一数据源
课程与日程各存一份,首页与课表**都是只读投影**,不持有独立副本。日程改动后首页与课表下次渲染即生效。

### 6.2 校历 → 周次 → 内容的推导链

```
系统当前日期
   |
   v
周次 = floor((今天 - 2026-09-07) / 7) + 1        <- 校历常量
   |                        星期 = 今天是周几 (1-7)
   v
过滤课程: slots 中满足 weekStart <= 周次 <= weekEnd 且 weekday == 星期 的时段
   |
   v
首页 B「今日课程」 = 上述结果按节次排序
课表「当前周视图」 = 上述结果按 weekday 分列铺进网格
   |
   v
日程按 date 与今天/本周区间比较 -> 首页 C「下一个安排」、首页 D「今日待办」、日程页筛选结果
```

### 6.3 跨模块的具体关系
- **校历 → 课表**:周次决定网格显示哪一周,也决定哪些课程时段存在(第 9 周后"可靠性中的机器学习"不再出现,第 6 周起"物流运输"才出现)
- **校历 → 首页**:横幅的"第 N 周 / 共 19 周"与进度条、今日课程过滤结果,全部来自同一套周次计算,两处不会出现口径不一致
- **课表 → 日程**:点课表空白格新建日程时,预填 日期 = 该列对应日期、`startTime` = 该节次起始时间、`endTime` = 该节次结束时间;用户可改
- **日程 → 首页**:今日待办与"下一个安排"完全来自日程数据;勾选完成即时反映
- **课程 → 首页**:"下一个安排"是课程时段与日程共同竞争最早未来项,不只看日程
- **无固定时间课程**(人工智能安全与伦理):不进课表网格、不进首页今日课程,只在课表页顶部"无固定时间课程"条中展示
- **课程与日程不自动联动**:在课上建的一条日程不会因为课程取消而消失,第一版不做课程与日程的父子关系

---

## 7. 第一版必须完成的功能

### 7.1 基础
1. 单 `index.html` 双击可开,离线可用,无外部依赖
2. 三视图 SPA 导航,状态保留
3. `localStorage` 持久化,刷新 / 关闭重开数据不丢
4. 首次打开自动预置附录 A 的 4 门课程
5. 桌面 + 移动双端可用,响应式断点见 2.2

### 7.2 校历与课表
6. 按 `2026-09-07` 起算、共 19 周自动计算当前周次;超出范围显示"假期 / 非教学周"
7. 桌面周视图网格:节次轴 1–14 行 × 周一~周日 7 列,课程色块按起止节次跨行渲染,同一节课的连续节次合并为一块
8. 移动端单日视图:日期标签条(横向滚动、有课日期带小圆点、默认选中今天)+ 当日纵向时间轴
9. 周次切换:上一周 / 下一周 / 回到本周;第 1 周禁用上一周、第 19 周禁用下一周;移动端切周保持同一星期几
10. 按当前周次自动过滤课程时段
11. 点空白格(桌面 hover 显示 "+" 角标)/ 点空闲行加号(移动端)→ 新建日程并预填日期与节次时间
12. 点课程色块 → 课程详情弹层,合并展示该课全部时段
13. 桌面周视图渲染当前时间红线(仅当查看的是本周时);移动端不画红线,改由条目"进行中"标记表达
14. 课表页顶部"无固定时间课程"条
15. 今天所在列 / 今天标签高亮

### 7.3 日程
16. 新建 / 编辑 / 删除(二次确认)/ 勾选完成
17. 筛选:今天 / 本周 / 全部 / 已完成
18. 列表按日期分组,分组标题带日期、星期、周次;移动端分组标题吸顶
19. 逾期项(日期已过且未完成)在"全部"筛选下时间文字标红并标注"已逾期"
20. 移动端右下角 FAB 新建;桌面端顶部按钮新建
21. 空状态文案

### 7.4 首页
22. 学期横幅(日期、星期、周次、进度条)
23. 今日课程列表,含进行中 / 未开始 / 已结束三态
24. 下一个安排 + 倒计时,60 秒刷新
25. 今日待办 + 完成计数 + 行内快速勾选 + 行内添加

### 7.5 课程编辑
26. 课程详情弹层提供"编辑"入口,可改名称、教师、校区、上课方式、颜色、地点
27. 时段列表可逐条增 / 改 / 删(周次区间、星期、起止节次、地点)
28. 删除课程二次确认(红色危险按钮)
29. 移动端编辑用分步抽屉:第一步基础信息,第二步时段列表;字段单列,标签置于输入框上方

### 7.6 备份
30. 导出当前数据为 JSON 文件下载
31. 导入 JSON:校验结构后覆盖,失败不破坏现有数据
32. 恢复预置课表(二次确认)、清空全部数据(二次确认)
33. 备份弹层显示当前存储用量与课程 / 日程条数

### 7.7 交互规范
34. 增删改成功 → 右下角 toast「已保存 / 已完成 / 已删除」,2 秒消失;失败 → 红色 toast 常驻 4 秒
35. 桌面键盘快捷键:课表 ←/→ 切周、日程 N 新建、弹层 Esc 关闭;用 `matchMedia('(hover: hover)')` 判定,移动端不启用
36. 触控基线:可点元素 ≥44×44px,相邻间距 ≥8px;不出现"仅 hover 可达"的功能
37. 日期 / 时间用原生 `input[type=date]` / `[type=time]`,移动端调起系统选择器
38. `viewport` 设为 `width=device-width, initial-scale=1`,不禁用用户缩放
39. 视觉基调:浅色主题、蓝色主色、圆角 10px、卡片极浅阴影;课程色块 4 色循环取色,可被用户设置覆盖

---

## 8. 第一版暂时不做的功能

| 类别 | 不做的内容 |
|---|---|
| 账号与协作 | 用户注册、登录、权限、多用户、任何社交 / 分享功能 |
| 数据存储 | 数据库、后端服务、云端同步、多设备自动同步(只能靠手动导出导入) |
| 校历逻辑 | 节假日与调休自动跳课、校历红字参与排课、考试周与寒暑假的独立视图、第二 / 第三学期切换(仅秋季学期) |
| 日程能力 | 优先级、重复规则(每天 / 每周 / 每月)、子任务、标签体系(仅固定 4 分类)、附件与图片、日程之间的关联或依赖 |
| 提醒 | 系统级推送通知、浏览器 Notification API、后台定时提醒(纯前端只能做到"打开页面时看到倒计时") |
| 视图 | 月视图、日视图(桌面端)、学期总览热力图、导出为图片 / PDF / ICS 日历订阅 |
| 课程 | 从教务系统自动抓取、导入学校课表文件、调课 / 代课临时变更、课程与日程的父子联动 |
| 统计 | 完成率统计、时间投入分析、周报 |
| 界面 | 深色模式、多语言、自定义主题色板、拖拽调整日程、手势滑动删除、PWA 离线安装包与 Service Worker |
| 其他 | 撤销 / 重做栈、操作历史、数据版本冲突合并 |

---

## 9. 可以实际检查的验收标准

前置:在 Chrome 与 iPhone Safari(或 DevTools 移动模拟 375×667)各执行一遍。以下"今天"指系统日期。

### A. 安装与持久化
1. 双击 `index.html` 能在浏览器中正常打开并渲染首页,控制台无红色报错,断网状态下同样正常
2. 首次打开后进入课表页,能看到附录 A 的 4 门课程(其中 3 门有时段色块)
3. 新建一条日程"验收测试"→ 按 Ctrl+R 刷新页面 → 该日程仍在
4. 关闭浏览器标签页后重新打开同一文件 → 步骤 3 的日程仍在,且首页今日待办可见
5. 在课表页把某课程地点改为"测试楼101"并保存 → 刷新 → 课表色块与课程详情均显示新地点

### B. 校历与周次
6. 系统日期设为 2026-09-23 → 首页横幅显示"第 3 周 / 共 19 周",进度条约 15%
7. 系统日期设为 2026-09-07 → 显示"第 1 周";设为 2027-01-17 → 显示"第 19 周"
8. 系统日期设为 2027-03-01 → 显示"假期 / 非教学周",今日课程区显示空状态
9. 课表切到第 1 周 → 网格中周一 1-2 节有"可靠性中的机器学习",周五 2-5 节有"科学研究与学术写作",周一至周日均无"物流运输"
10. 课表切到第 9 周 → 周一 1-2 节的"可靠性中的机器学习"消失(其范围 1-9 周含第 9 周,应显示);切到第 10 周 → 确认该课不再显示
11. 课表切到第 6 周 → 周一出现 8-9 节与 10 节两块"物流运输"(8-9 与 10 因周次范围不同而分块),周二出现 8-10 节连堂一块
12. 课表切到第 11 周 → 周一只剩 8-9 节的"物流运输"(10 节时段止于第 10 周)
13. 在第 1 周时"上一周"按钮为禁用态;在第 19 周时"下一周"按钮为禁用态;点"回到本周"跳回当前周次

### C. 课表交互
14. 桌面端鼠标悬停在某空白格 → 该格出现淡蓝底与 "+" 角标;点击 → 弹出新建日程弹层,日期等于该列日期、开始/结束时间等于该节次时间,标题输入框已获得焦点
15. 在步骤 14 的弹层里填标题并保存 → 关闭弹层、出现 toast,切到日程页能在对应日期分组看到该条
16. 点任一课程色块 → 详情弹层显示课程名、教师、以及该课全部时段(每段含周次、星期、节次、时间、地点);点"关闭"或遮罩或 Esc 可关闭
17. 查看"物流运输"详情 → 弹层内列出 3 个时段
18. 课表页顶部能看到"人工智能安全与伦理 · 韦星星 · 全线上教学",且它不出现在任何网格格子里
19. 桌面周视图中,当前节次时间有红色细线标出;把课表切到其他周 → 红线消失
20. 移动端(375px 宽)→ 无 7 列网格,显示日期标签条 + 当日纵向时间轴;今天对应日期标签为选中态;空闲时段行有 "+" 按钮,点击可新建日程
21. 移动端在日期标签条上左右滑动可见满 7 天;点某个有课的日期 → 下方时间轴显示该天课程

### D. 日程
22. 新建日程:标题留空点保存 → 阻止保存并在标题下方出现红字提示
23. 新建日程:开始 15:00、结束 14:00 → 提示"结束时间需晚于开始时间",无法保存
24. 只填日期不填时间保存 → 列表中该条显示"全天",且排在同日期有时间的条目之后
25. 筛选"今天" → 只显示日期为今天的日程;筛选"本周" → 只显示当前教学周周一~周日内的;筛选"已完成" → 只显示 `done=true` 的
26. 点列表项左侧复选框 → 不打开弹层,条目原地变为删除线 + 灰色,出现 toast;再点一次恢复未完成
27. 打开"已完成"筛选,确认步骤 26 中勾选完成的条目出现在这里,且不再出现在"全部"里
28. 移动端点列表项主体 → 打开编辑弹层,弹层从底部滑出、按钮吸底、内部可滚动;删除按钮在弹层内
29. 点删除 → 出现二次确认弹层,文案含该日程标题;点"取消"→ 条目仍在;再删一次点"确认"→ 条目消失并淡出,toast 提示
30. 系统日期设为 2026-09-23,新建一条 2026-09-20 且未完成的日程 → 在"全部"筛选下其时间文字为红色并标注"已逾期"
31. 日程分类选"作业" → 列表中该条的分类标签颜色与首页、弹层内 chip 三处一致
32. 所有筛选下都无数据时 → 显示空状态文案与"去新建"入口,不出现空白页

### E. 首页
33. 首页四个区块(横幅 / 今日课程 / 下一个安排 / 今日待办)全部渲染,标题与数字与实际操作结果一致
34. 今天是 2026-09-23 周三 → 若当天无课,今日课程区显示"今天没有课,好好安排一下";把系统日期改为 2026-09-11(周五,第 1 周)→ 今日课程显示"科学研究与学术写作 08:50-12:15 R1-4093"
35. 当前时间落在某课程时段内 → 该行有"进行中"标记;已过该时段 → 整行降透明度
36. "下一个安排"显示的是未来最近的课程或未完成日程二者中更早的一个;把它完成或删除后,该卡片在下次渲染时更新为下一个候选
37. 修改系统时间或等待 1 分钟,倒计时数字发生变化(不是固定值)
38. 点"今日课程"某行 → 跳到课表视图且显示当前周
39. 今日待办区点"+ 添加待办" → 弹层日期已预填为今天
40. 移动端首页顺序为:横幅 → 下一个安排 → 今日课程 → 今日待办,卡片全宽,无横向滚动条

### F. 课程编辑
41. 课程详情弹层点"编辑" → 表单预填该课全部字段,修改教师名保存 → 首页与课表下次渲染即显示新值
42. 编辑课程时新增一个时段(如 周三 11-12 节 地点 X)保存 → 课表第 1-9 周的周三 11-12 节出现该色块
43. 编辑课程时删除一个时段保存 → 对应色块从课表消失
44. 删除课程 → 二次确认后,该课从课表、首页、"无固定时间课程"条中全部消失
45. 移动端课程编辑为分步抽屉,可在两步间来回切换,长表单可滚动且吸底按钮不被键盘遮挡

### G. 备份
46. 备份弹层显示当前存储用量与"课程 N 门 · 日程 M 条",数字与实际一致
47. 点"导出 JSON" → 浏览器下载一个 `.json` 文件,内容含全部课程与日程
48. 新建若干日程 → 点"清空全部数据"并确认 → 日程全部消失、课表为空;点"恢复预置课表" → 附录 A 的 4 门课程重新出现
49. 选一个非 JSON 文件导入 → 出现红色错误 toast,现有数据完全未变
50. 用步骤 47 导出的文件在"数据已被清空"的状态下导入 → 课程与日程完整还原,刷新后仍在

### H. 响应式与触控
51. 浏览器窗口从 1400px 连续缩到 360px → 无横向滚动条、无元素重叠、无文字被裁切;断点处导航从顶部切到底部、课表从网格切成单日视图
52. 移动端所有可点元素(复选框、chip、行、按钮)点击区不小于 44×44px,目测相邻元素间距不粘连
53. 移动端不存在"只有 hover 才能触发"的操作:课表新建入口在移动端表现为常驻的空闲行 "+" 按钮
54. 移动端底部 Tab 不被 iPhone 手势条遮挡,内容区最后一条可完整滚出视野
55. 移动端点日期输入框 → 调起系统原生日期选择器
56. 桌面端键盘快捷键生效(课表 ←/→ 切周、日程 N 新建、Esc 关弹层);移动端不响应 N 键(因无实体键盘,不构成问题),且在输入框内打字不会误触发桌面快捷键

---

## 附录 A · 预置课表数据(2026–2027 秋季学期)

学期常量:第 1 周周一 = `2026-09-07`,共 `19` 教学周,学期止于 `2027-01-17`。

| # | 课程代码 | 课程名称 | 班级 | 教师 | 上课方式 | 校区 / 开课单位 | 时段 |
|---|---|---|---|---|---|---|---|
| 1 | D141011A03 | 可靠性中的机器学习(博) | 1 | 刘杰 | 面授讲课 | 学院路校区 / 011400 可靠性与系统工程学院 | 1-9 周 · 周一 · 1-2 节 · B118 |
| 2 | D411026B01 | 人工智能安全与伦理 | 01 | 韦星星 | 全线上教学 | 学院路校区 / 014100 人工智能学院 | **无固定时间**,`slots = []` |
| 3 | D571026C31 | 科学研究与学术写作 | 01 | 黄雪飞、张莹莹 | 面授讲课 | 杭州国际创新研究院 / 015700 | 1-8 周 · 周五 · 2-5 节 · 计算机房(R1-4093) |
| 4 | T130032042 | 物流运输模型与算法 | 国新院班 | 周宇 | 面授讲课 | 杭州国际创新研究院 / 011300 交通科学与工程学院 | 见下方 3 个时段 |

课程 4 的时段:

| 时段 | 周次 | 星期 | 节次 | 时间 | 地点 |
|---|---|---|---|---|---|
| ① | 6-11 周 | 周一 | 8-9 节 | 15:50-17:25 | 教学一号楼3003 |
| ② | 6-10 周 | 周一 | 10 节 | 17:30-18:15 | 教学一号楼3003 |
| ③ | 6-10 周 | 周二 | 8-10 节 | 15:50-18:15 | 教学一号楼3003 |

数据交叉校验(三处均自洽,可用于验证周次计算实现是否正确):
- 课程 1 首次上课 2026-09-07 = 第 1 周周一 ✓
- 课程 3 首次上课 2026-09-11 = 第 1 周周五 ✓
- 课程 4 首次上课 2026-10-12 = 第 6 周周一 ✓

## 附录 B · 教学作息时间表(节次 → 时间)

| 节次 | 时间 | 节次 | 时间 |
|---|---|---|---|
| 1 | 08:00-08:45 | 8 | 15:50-16:35 |
| 2 | 08:50-09:35 | 9 | 16:40-17:25 |
| 3 | 09:50-10:35 | 10 | 17:30-18:15 |
| 4 | 10:40-11:25 | 11 | 19:00-19:45 |
| 5 | 11:30-12:15 | 12 | 19:50-20:35 |
| 6 | 14:00-14:45 | 13 | 20:40-21:25 |
| 7 | 14:50-15:35 | 14 | 21:30-22:15 |

节次区间 → 时间:起始时间取 `periodStart` 的开始时刻,结束时间取 `periodEnd` 的结束时刻。

## 附录 C · 校历中已标注但 v1 不处理的日期

国庆 10-01~10-07、中秋 2026-09-25、校运会 10-16~10-17、校庆日 10-25、元旦 2027-01-01 等红字日期。按已确认口径(校历规则 A),这些日期**不参与跳课与调课判断**,课表照常按周次渲染。

## 附录 D · 存储键与结构

```
localStorage key: schedule-app-v1
{
  "courses": [ { "id", "name", "teacher", "campus", "mode", "color",
                 "slots": [ { "weekStart", "weekEnd", "weekday",
                              "periodStart", "periodEnd", "location" } ] } ],
  "events":  [ { "id", "title", "date", "startTime", "endTime",
                 "location", "category", "note", "done", "createdAt" } ],
  "settings": { "semesterStart": "2026-09-07", "totalWeeks": 19, "schemaVersion": 1 }
}
```
